# Qrate client logos

Add the eight logo files referenced in `index.html` here. Transparent PNG or WebP files work best. The ticker applies a white treatment automatically and restores colour on hover.
