# Qrate photo assets

The supplied photographs have been exported here using these names:

- `hero-1.webp` through `hero-5.webp` (with matching `.jpg` fallbacks)
- `bts-1.webp` through `bts-6.webp` (with matching `.jpg` fallbacks)

The hero images are 1920px wide and the work-grid images are 1400px wide. WebP files use quality 78; progressive JPG copies provide the fallback.
